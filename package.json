{
  "name": "marketing-media-tree-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r dist ../dist",
    "vercel-build": "cd frontend && npm install && npm run build && cp -r dist ../dist"
  }
}
